name: Python Checks VMC Sandbox

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/sandbox/**"
      - "Libraries/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/python.yml
    with:
      project: "VMC/sandbox"