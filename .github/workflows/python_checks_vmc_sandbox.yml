name: Python Checks VMC Sandbox

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/sandbox/**"
      - "Libraries/**"
      - "requirements.txt"
      - "package-lock.json"

jobs:
  vmc-sandbox:
    uses: ./.github/workflows/python_checks.yml
    with:
      project: "VMC/sandbox"