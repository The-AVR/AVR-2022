name: Python Checks VMC VIO

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/vio/**"
      - "Libraries/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/templates/python.yml
    with:
      project: "VMC/vio"