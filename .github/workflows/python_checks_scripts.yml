name: Python Checks scripts

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "scripts/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/templates/python.yml
    with:
      project: "scripts"