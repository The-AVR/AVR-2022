name: Python Checks GUI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "GUI/**"
      - "Libraries/**"
      - "requirements.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/python_checks.yml
    with:
      project: "GUI"