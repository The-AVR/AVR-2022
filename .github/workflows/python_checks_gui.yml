name: Python Checks GUI

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "GUI/**"
      - "Libraries/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/templates/python.yml
    with:
      project: "GUI"