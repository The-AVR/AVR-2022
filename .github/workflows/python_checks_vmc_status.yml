name: Python Checks VMC Status

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/status/**"
      - "Libraries/**"
      - "requirements.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/python_checks.yml
    with:
      project: "VMC/status"