name: Build Container FCM

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/fcm/**"
      - "Libraries/**"
  push:
    branches:
      - main
    paths:
      - "VMC/fcm/**"
      - "Libraries/**"

jobs:
  check:
    uses: ./.github/workflows/templates/container.yml
    with:
      image: "fcm"