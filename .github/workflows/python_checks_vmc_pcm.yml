name: Python Checks VMC PCM

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/pcm/**"
      - "Libraries/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/python_checks.yml
    with:
      project: "VMC/pcm"