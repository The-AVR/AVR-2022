name: Python Checks VMC Fusion

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/fusion/**"
      - "Libraries/**"
      - "requirements-dev.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/templates/python.yml
    with:
      project: "VMC/fusion"