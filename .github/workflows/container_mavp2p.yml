name: Build Container Mavp2p

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/mavp2p/**"
  push:
    branches:
      - main
    paths:
      - "VMC/mavp2p/**"

jobs:
  check:
    uses: ./.github/workflows/container.yml
    with:
      image: "mavp2p"