name: Python Checks VMC Apriltag

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/apriltag/python/**"
      - "Libraries/**"
      - "requirements.txt"
      - "package-lock.json"

jobs:
  check:
    uses: ./.github/workflows/python_checks.yml
    with:
      project: "VMC/apriltag/python"