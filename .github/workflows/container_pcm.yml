name: Build Container PCM

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
      - develop
    paths:
      - "VMC/pcm/**"
      - "Libraries/**"
  push:
    branches:
      - main
      - develop
    paths:
      - "VMC/pcm/**"
      - "Libraries/**"

jobs:
  pcm:
    uses: ./.github/workflows/container.yml
    with:
      image: "pcm"