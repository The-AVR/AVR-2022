name: Build Container PCM

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/pcm/**"
      - "Libraries/**"
  push:
    branches:
      - main
    paths:
      - "VMC/pcm/**"
      - "Libraries/**"

jobs:
  pcm:
    uses: ./.github/workflows/container.yml
    with:
      image: "pcm"