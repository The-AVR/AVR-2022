name: Build Container Fusion

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - "VMC/fusion/**"
      - "Libraries/**"
  push:
    branches:
      - main
    paths:
      - "VMC/fusion/**"
      - "Libraries/**"

jobs:
  check:
    uses: ./.github/workflows/templates/container.yml
    with:
      image: "fusion"