{
  "dependencies": {
    "autoprefixer": "^10.4.0",
    "hugo-extended": "0.95.0",
    "postcss": "^8.4.5",
    "postcss-cli": "^9.0.2"
  },
  "devDependencies": {
    "broken-link-checker": "^0.7.8",
    "prettier": "^2.5.1",
    "start-server-and-test": "^1.14.0"
  },
  "scripts": {
    "build": "hugo --gc --cleanDestinationDir && python postprocess.py",
    "check-formatting": "prettier --check ./content/",
    "check-links-exec": "blc -reo http://localhost:1313",
    "check-links": "start-server-and-test server http://localhost:1313 check-links-exec",
    "server": "hugo server -p 1313",
    "format": "prettier --write content/"
  }
}
