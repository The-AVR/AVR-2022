{
  "dependencies": {
    "autoprefixer": "^10.4.7",
    "cheerio": "^1.0.0-rc.11",
    "glob": "^8.0.3",
    "hugo-extended": "0.98.0",
    "postcss": "^8.4.13",
    "postcss-cli": "^9.1.0",
    "sync-request": "^6.1.0",
    "toml": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "open-cli": "^7.0.1"
  },
  "scripts": {
    "build": "hugo --cleanDestinationDir --verbose && node postprocess.js",
    "check-formatting": "prettier --check content/",
    "format": "prettier --write content/",
    "server": "open-cli 'http://localhost:1313' && hugo server -p 1313"
  }
}
