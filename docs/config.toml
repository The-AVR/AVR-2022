baseURL = "/"
title = "Bell VRC Documentation"
enableRobotsTXT = true
theme = "docsy"

disableKinds = ["RSS"]

# Will give values to .Lastmod etc.
enableGitInfo = true

contentDir = "content/en"
defaultContentLanguage = "en"
defaultContentLanguageInSubdir = false

# Highlighting config
pygmentsCodeFences = true
pygmentsUseClasses = false
# Use the new Chroma Go highlighter in Hugo.
pygmentsUseClassic = false
#pygmentsOptions = "linenos=table"
# See https://help.farbox.com/pygments.html
pygmentsStyle = "monokai"

# Needed for some shortcodes
[markup]
[markup.goldmark]
[markup.goldmark.renderer]
unsafe = true

# Image processing configuration.
[imaging]
resampleFilter = "CatmullRom"
quality = 75
anchor = "smart"

[outputs]
section = ["HTML", "print", "RSS"]

[params]
copyright = "Bell Flight"
privacy_policy = "https://www.bellflight.com/legal/privacy-policy"

# First one is picked as the Twitter card image if not set on page.
# images = ["images/project-illustration.png"]

# A link to latest version of the docs. Used in the "version-banner" partial to
# point people to the main doc site.
url_latest_version = "https://bellflight.github.io"

# Repository configuration (URLs for in-page links to opening issues and suggesting changes)
github_repo = "https://github.com/bellflight/VRC-2022"

# Specify a value here if your content directory is not in your repo's root directory
github_subdir = "docs"

# Uncomment this if you have a newer GitHub repo with "main" as the default branch,
# or specify a new value if you want to reference another branch in your GitHub links
github_branch = "main"

# Enable Algolia DocSearch
algolia_docsearch = false

# Enable Lunr.js offline search
offlineSearch = true

# Enable syntax highlighting and copy buttons on code blocks with Prism
prism_syntax_highlighting = false

# User interface configuration
[params.ui]
# Set to true to disable breadcrumb navigation.
breadcrumb_disable = false
# Set to true to disable the About link in the site footer
footer_about_disable = false
# Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top navbar
navbar_logo = true
# Set to true if you don't want the top navbar to be translucent when over a `block/cover`, like on the homepage.
navbar_translucent_over_cover_disable = false
# Enable to show the side bar menu in its compact state.
sidebar_menu_compact = true
# Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
sidebar_search_disable = false
# Make the sidebar menu foldable
sidebar_menu_foldable = true

[params.ui.feedback]
enable = false

# Adds a reading time to the top of each doc.
# If you want this feature, but occasionally need to remove the Reading time from a single page, 
# add "hide_readingtime: true" to the page's front matter
[params.ui.readingtime]
enable = true

[params.links]
[[params.links.user]]
name = "Bell Flight"
url = "https://bellflight.com"
icon = "fa fa-helicopter"
desc = "Welcome to the Future of Flight"

[[params.links.user]]
name = "Job Opportunities"
url = "https://textron.taleo.net/careersection/bell/jobsearch.ftl?lang=en"
icon = "fa fa-briefcase"

[[menu.main]]
name = "GitHub"
weight = 50
pre = "<i class='fab fa-github'></i>"
url = "https://github.com/bellflight/VRC-2022/"

[[menu.main]]
name = "Microsoft Teams"
weight = 100
pre = "<i class='fa fa-users'></i>"
url = "https://teams.microsoft.com/l/team/19%3a21c615dc691c4f289fb9fda44c862df3%40thread.tacv2/conversations?groupId=6baa0f71-7580-4c72-905f-2b6ee58685c3&tenantId=771923a0-2465-44c5-8531-b9366e228aad"
